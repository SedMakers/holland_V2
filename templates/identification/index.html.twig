{% extends 'base.html.twig' %}

{% block title %}Hello IdentificationController!{% endblock %}

{% block body %}
    <div class="loginIn">

        {{ form_start(form) }}
        <div class="login">
            <div>
                {{ form_label(form.prenom) }}
                {{ form_widget(form.prenom) }}
            </div>
            <div>
                {{ form_label(form.nom) }}
                {{ form_widget(form.nom) }}
            </div>
            <div>
                {{ form_label(form.mail) }}
                {{ form_widget(form.mail) }}
            </div>
            {#    {{ form_label(form.age) }} #}
            {#    {{ form_widget(form.age) }} #}
        </div>

        {{ form_rest(form) }}
        <br>
        <button type="submit" class="boutonStart">Enregistrer</button>

        {{ form_end(form) }}

    </div>
{% endblock %}
